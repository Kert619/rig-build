export default defineAppConfig({
  ui: {
    colors: {
      neutral: "neutral",
    },
    input: {
      slots: {
        root: "w-full",
      },
    },
    button: {
      slots: {
        base: "cursor-pointer",
      },
    },
  },
});
